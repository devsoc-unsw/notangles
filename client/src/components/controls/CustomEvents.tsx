import React, { useState } from 'react';
import { Box, Button } from '@mui/material';

const CustomEvents = ({}) => {
  return (
    <div style={{ display: 'flex' }}>
      <Button>
        <Box>CUSTOM USER EVENTS</Box>
      </Button>
    </div>
  );
};

export default CustomEvents;
