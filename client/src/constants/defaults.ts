const defaults: Record<string, any> = {
  is12HourMode: false,
  isDarkMode: false,
  userID: '',
  accessToken: '',
  userPicture: '',
  selectedCourses: [],
  selectedClasses: {},
};

export default defaults;
