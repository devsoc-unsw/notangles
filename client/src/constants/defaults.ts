const defaults: Record<string, any> = {
  is12HourMode: false,
  isDarkMode: false,
  isSquareEdges: false,
  userID: '',
  accessToken: '',
  userPicture: '',
  selectedCourses: [],
  selectedClasses: {},
  hasShownInfoMessage: false,
};

export default defaults;
